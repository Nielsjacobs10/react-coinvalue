import React, { Component } from 'react';
import './coinview';

class Detail extends Component {
    render() {
        return <h1 className="login">Grafiek:</h1>;
      }
    }
export default Detail;